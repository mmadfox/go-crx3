Encodes a Chrome extension file into a Base64 string for use in data URLs, embedding, or transmission.

<usage>
Use this tool when the user needs to convert a binary extension file (e.g., .crx, .zip, or individual assets) into a Base64-encoded string. This is useful for embedding files in code, configs, or sending binary data as text.
</usage>

<example>
- "Encode the packed extension into base64"
- "Convert extension.zip to base64 string"
- "I need a base64 representation of manifest.json"
- "Encode the background.js file for embedding in HTML"
</example>